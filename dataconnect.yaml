specVersion: "v1beta"
serviceId: "SKZ Universe"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "SKZ Universe-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
